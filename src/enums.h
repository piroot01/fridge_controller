#pragma once

enum TOP_LVL_STATE
{
    INFO, MENU, SETTING
};

enum MIDDLE_LVL_STATE
{
    TEMPERATURE, COLOR, MODE, ALARM, TIME, EXIT
};

enum STRIP_MODES
{
    ON, OFF, AUTO
};

enum SET_TIME
{
    HOUR, MINUTE
};